from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Predicting the demand of rides in NYC, by dividing NYC map into cluster, using unsupervised & supervised ML algorithms.',
    author='Akshat Sharma',
    license='MIT',
)
